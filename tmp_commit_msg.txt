feat(T07): Vanta.NET 3D 背景 + GSAP ScrollTrigger 滚动揭示动画 + GPU 降级策略

- 新增 src/composables/useVantaBackground.ts：MaybeRef<HTMLElement> 驱动的
  Vanta.NET 初始化入口，动态 import('three') + import('vanta/dist/vanta.net.min.js')
  拆出独立 chunk，不污染其他路由页面；5 层 GPU 降级检测（prefers-reduced-
  motion / hardwareConcurrency<=2 / deviceMemory<=2 / viewport<768 /
  WebGL 不可用）命中即走 fallback；watch useTheme.resolved 在深浅切换
  时调 setOptions（vanta 模式）或 class toggle（fallback 模式）；暴露
  mode/error/destroyEffect/remount API
- 新增 src/composables/useScrollReveal.ts：基于 GSAP + ScrollTrigger 的
  滚动进入揭示动画，root 容器内扫描 [data-reveal] 子元素并 apply fade-in-up
  + blur 4px→0（0.55s，power2.out，start top 88%）；data-reveal 属性值解析
  为数字作错落延迟；prefers-reduced-motion 直接跳过；unmount 精确 kill
  关联的 ScrollTrigger；registerPlugin 只执行一次（全局 singleton 开关）
- tokens.css 末尾追加 .hero-bg-vanta / .hero-bg-fallback 样式：两团径向紫雾
  + 点阵 SVG 数据背景（light/dark 各自透明度），与品牌色 HSL 对齐；容器
  绝对定位 inset-0 z-[-1] pointer-events-none overflow-hidden
- HomePage 结构重构：Hero 外层包 relative overflow-hidden rounded-2xl
  -mx-2 z-0 建立层叠上下文，内嵌 <div ref="heroBgEl" /> 作为 Vanta/fallback
  目标；四个主板块加 data-reveal（包括 Hero 容器本体、统计 chip、最近
  作品 section、最近博客 section），卡片与列表项用 data-reveal="i*0.04"
  错落；首页根 section 挂 ref="pageRoot" 调用 useScrollReveal
- AboutPage / PortfolioPage / TimelinePage 均新增 rootRef + useScrollReveal，
  给主要 section/卡片/时间线节点加 data-reveal 错落标签；BlogPage、
  ContactPage、NotFoundPage 保持朴素不动效
- 新增 src/shims-modules.d.ts：为 `three` 与 `vanta/dist/vanta.net.min.js`
  提供宽松 ambient 类型声明，修复 vue-tsc -b 报 7016（Could not find declaration
  file）
- 修复 useVantaBackground 内 useTheme 字段名误写 resolvedTheme→resolved，
  修正 TS2339
- npm run build 通过（15.14s，three 独立 chunk 734KB / vanta 13.7KB 仅非降级
  首页才请求；GSAP+ScrollTrigger 合并 chunk 115KB 路由懒加载）
