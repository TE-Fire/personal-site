feat(T06): Hero 终端打字机 + CTA 淡入 + AI 引入文案

- 新增 src/composables/useTerminal.ts：MaybeRef<TerminalStep[]> 驱动的共享
  步进协程，支持 command 逐字打字（带 jitter）、output 逐行瞬时、
  blank/pause 空行与停顿；暴露 lines / status / isTyping / isDone /
  restart / skipToEnd；mount 时若 prefers-reduced-motion 立即 skipToEnd
- HomePage Hero 终端静态结构替换为 useTerminal composable 消费：
  whoami → cat motto → ls projects --only-highlight → head blog latest
  → ./ai --intro --vibe --color=purple 五步命令流
- 终端头部增加 replay + skip 两个交互按钮（打字中时 replay disabled，
  非 done 时 skip 出现）
- 终端底部增加 CTA 栏（查看作品 / 阅读博客 / 联系我），status='done'
  时经 translate-y + opacity fade-in 出现
- aboutMe 新增 tags: 4 条方向，补齐 Hero 终端 whoami 输出需要
- useTerminal 参数接受 MaybeRef<T[]>，允许 computed script，HomePage 据
  数据动态合成 script（包含 featured 3 项目与 3 博客文字）
- npm run build 通过：主包 151.51KB / HomePage 14.42KB（gzip 5.84KB）
