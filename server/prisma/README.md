/**
 * Prisma Schema 占位文件
 *
 * 正式接入数据库时，替换为完整 schema.prisma 然后执行：
 *   1. npx prisma migrate dev --name init
 *   2. npm run prisma:generate
 *
 * 示例表结构：
 *   · User
 *   · Post
 *   · Category
 *   · Tag
 *   · PostTag (多对多)
 */
// 此处为占位，正式 schema 放到 server/prisma/schema.prisma
